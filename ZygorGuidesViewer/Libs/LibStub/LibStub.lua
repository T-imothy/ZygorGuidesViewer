-- LibStub is bootstrapped by Compat_112.lua before embeds.xml.
-- Keep this file for embedded-library compatibility.
if not LibStub then
    error("Zygor Classic compatibility bootstrap failed to create LibStub.")
end
